// Instalar o teclado padrão Americano e aceitar acento
setxkbmap us -variant intl
setxkbmap -layout br
setxkbmap -model abnt2 -layout br

// Gerenciador do meu prompt no Ubuntu
sudo nano ~/.bashrc
source ~/.bashrc

// Onde Foi comfirado o meu teclado Padrão Americano e com acento
sudo gedit ~/.xprofile 
sudo chmod +x ~/.xprofile // ex

// MATAR PORTA
sudo fuser -k 3000/tcp

//  STATUS PORTA
sudo lsof -i :3000

// MONNITORAR PORRTA
sudo apt install htop
sudo apt install iftop
sudo apt install nethogs


// Procurar para saber se porta esta ativa
netstat -an | grep "3000"
netstat -tuln | grep 3000

------------------------- PM2 --------------------------------------
pm2 stop all
pm2 kill
pm2 delete all
pm2 save
pm2 list
pm2 logs capixabay-back-dev
pm2 logs capixabay-back-dev --lines 100 > logs.txt
pm2 save
pm2 startup
cd ~/.pm2/logs/

-------  capixabay-back --------
// Verifica todos imports
npx tsc --noEmit